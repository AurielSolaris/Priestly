/* Priestly node configuration -- JSONX (JSONL records + JSONC comments).
   One JSON object per line; later lines override earlier keys. */

# identity
{"server_name": "priestly-node-1"}

# bind address (host + port on one record, trailing comma tolerated)
{"host": "localhost", "port": 8765,}

// To override just the port for a local run, append a line like:
// {"port": 9000}
